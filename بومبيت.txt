I am building an e-commerce backend using Node.js, Express, and MongoDB (Mongoose).

Here is what I have already completed:

✅ User Model:

* name (unique, required)
* email (unique, required, validated)
* password (hashed using bcrypt)
* role (user/admin)
* phone, address, age
* Password is hashed using a pre-save middleware

✅ Category Model:

* name (unique, required, min length)
* slug (generated using slugify if not provided)
* description
* parentCategory (self-reference for nested categories)
* image
* isActive
* slug handled in pre-save and pre-findOneAndUpdate middleware

✅ Product Model:

* name
* slug (auto-generated or user-provided)
* description
* price
* discount
* finalPrice (calculated automatically)
* stock
* category & subCategory (ObjectId refs)
* createdBy (User ref)
* images + mainImage
* rating
* isActive
* slug + finalPrice handled in middleware (save + update)

✅ Authentication System:

* Register API (with confirmPassword validation)
* Login API (bcrypt.compare)
* JWT token generation using a helper function
* Token includes: userId, name, role

✅ JWT Helper:

* Separate file in middleware folder
* createToken(userId, name, role)

---

🔜 What I want to implement next (in correct professional order):

1. Authentication Middleware:

   * Verify JWT token from headers
   * Attach user to req.user

2. Authorization Middleware:

   * Role-based access control (admin/user)

3. Global Error Handler:

   * Centralized error handling middleware

4. Then move to:

   * Category CRUD API (protected routes for admin)
   * Product CRUD API (protected routes for admin)

---

Important Notes:

* I want clean architecture (controllers, routes, middleware separation)
* Avoid repeating logic in controllers
* Follow best practices for scalability and security

---

Please help me implement:

1. Auth middleware (verify token)
2. Role authorization middleware
3. Global error handler
4. Then guide me step-by-step for Category CRUD API
